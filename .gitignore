# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.coverage
coverage.xml
htmlcov/
dist/
*.egg-info/

# Node
node_modules/
web/dist/
# The intermediate `npm run api` pipes into openapi-typescript. The generated client
# (web/src/api/schema.d.ts) is committed; this is not.
web/openapi.json

# Editors
*.swp
*.swo

# Local
.env
*.sqlite
*.sqlite-wal
*.sqlite-shm
# what `alembic upgrade head` writes when run by hand from packages/core
*.db
*.db-wal
*.db-shm

# Berkeley Mono is licensed, not redistributable, and is mounted at runtime rather than
# committed. The directory and its README are tracked; the faces never are.
web/public/fonts/berkeley/*.woff2
