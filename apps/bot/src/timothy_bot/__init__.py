"""Timothy's Discord bot: a gateway relay over the backend API.

Holds no domain logic and never writes the database — every decision is the backend's.
"""

__all__ = ["__version__"]

__version__ = "0.1.0"
